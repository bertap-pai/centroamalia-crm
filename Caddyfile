# NOTE: This Caddyfile is NOT needed for the sub-path deployment at intranet.centroamalia.com/crm/
# because the existing server already handles TLS for that domain.
#
# Only use this if you ever want to run the CRM on a completely separate domain
# with its own TLS certificate (e.g. crm.example.com pointing only to this Docker setup).
#
# For the current setup, configure your existing Apache or Nginx server to proxy /crm/ instead.
# See the deployment guide in the issue plan document.

# example.com {
#     reverse_proxy localhost:8080
# }
